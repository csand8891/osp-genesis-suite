<UserControl x:Class="RuleArchitect.DesktopClient.Views.SoftwareOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RuleArchitect.DesktopClient.Views"
             xmlns:vm="clr-namespace:RuleArchitect.DesktopClient.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:RuleArchitect.DesktopClient.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:SoftwareOptionsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource MaterialDesignPaper}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <UserControl.Resources>
        <!--<converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>-->
        <converters:NullToBooleanConverter x:Key="NullToFalseConverter" FalseForNull="True"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
            <ToolBarTray Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}">
                <ToolBar Style="{DynamicResource MaterialDesignToolBar}" ClipToBounds="False">
                    <Button Command="{Binding LoadCommand}" ToolTip="Refresh Software Options" Style="{StaticResource MaterialDesignToolButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center"/>
                            <TextBlock Text="Load" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Separator/>
                    <Button Command="{Binding AddCommand}" ToolTip="Add New Software Option" Style="{StaticResource MaterialDesignToolButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlusBoxOutline" VerticalAlignment="Center"/>
                            <TextBlock Text="Add" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditCommand}" ToolTip="Edit Selected Software Option"
                            IsEnabled="{Binding SelectedSoftwareOption, Converter={StaticResource NullToFalseConverter}}" Style="{StaticResource MaterialDesignToolButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PencilOutline" VerticalAlignment="Center"/>
                            <TextBlock Text="Edit" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" ToolTip="Delete Selected Software Option"
                            IsEnabled="{Binding SelectedSoftwareOption, Converter={StaticResource NullToFalseConverter}}"
                            Style="{StaticResource MaterialDesignToolButton}"
                            Foreground="{DynamicResource MaterialDesignValidationErrorBrush}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="DeleteOutline" VerticalAlignment="Center"/>
                            <TextBlock Text="Delete" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </ToolBar>
            </ToolBarTray>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="{Binding DetailPaneWidth, FallbackValue=0}"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Software Options" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,12"/>

                <materialDesign:Card Grid.Row="1" Padding="0" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                    <DataGrid ItemsSource="{Binding SoftwareOptions}" SelectedItem="{Binding SelectedSoftwareOption}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              SelectionMode="Single" 
                              Style="{StaticResource MaterialDesignDataGrid}" 
                              materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                              materialDesign:DataGridAssist.ColumnHeaderPadding="8"
                              BorderThickness="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding SoftwareOptionId}" Width="Auto" SortMemberPath="SoftwareOptionId"/>
                            <DataGridTextColumn Header="Primary Name" Binding="{Binding PrimaryName}" Width="2*" SortMemberPath="PrimaryName"/>
                            <DataGridTextColumn Header="Option #" Binding="{Binding PrimaryOptionNumberDisplay}" Width="*" SortMemberPath="PrimaryOptionNumberDisplay"/>
                            <DataGridTextColumn Header="Control System" Binding="{Binding ControlSystemName}" Width="*" SortMemberPath="ControlSystemName"/>
                            <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="Auto" SortMemberPath="Version"/>
                            <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModifiedDate, StringFormat='{}{0:g}'}" Width="*" SortMemberPath="LastModifiedDate"/>
                            <DataGridTextColumn Header="Modified By" Binding="{Binding LastModifiedBy}" Width="*" SortMemberPath="LastModifiedBy"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>
            </Grid>

            <Border Grid.Column="1" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1,0,0,0"
                    Visibility="{Binding IsDetailPaneVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <materialDesign:Card Padding="20" Margin="16" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ContentControl Grid.Row="0" Content="{Binding CurrentEditSoftwareOption}" Margin="0,0,0,16"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding SaveCommand}" Content="Save" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0" Width="80"/>
                            <Button Command="{Binding CancelEditCommand}" Content="Cancel" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80"/>
                        </StackPanel>
                    </Grid>
                </materialDesign:Card>
            </Border>
        </Grid>

        <Grid Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="2" Background="#44000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <materialDesign:Card UniformCornerRadius="10" Padding="16" VerticalAlignment="Center" HorizontalAlignment="Center" materialDesign:ElevationAssist.Elevation="Dp8">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Width="30" Height="30" Margin="0,0,12,0"/>
                    <TextBlock Text="Loading Options..." VerticalAlignment="Center" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>