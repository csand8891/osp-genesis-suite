<Window x:Class="RuleArchitect.DesktopClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RuleArchitect.DesktopClient"
        xmlns:vm="clr-namespace:RuleArchitect.DesktopClient.ViewModels"
        xmlns:views="clr-namespace:RuleArchitect.DesktopClient.Views" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="OSP Genesis Suite" Height="720" Width="1280"
        Icon="Genesis_flat_icon_256x256.ico"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="150" MaxWidth="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Background="{DynamicResource PrimaryHueMidBrush}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Margin="0,0,0,8">
                    <Separator Margin="8,16"/>
                    <Button Content="Logout" Command="{Binding LogoutCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="8,5" Height="40"/>
                </StackPanel>

                <TextBlock DockPanel.Dock="Top" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" Text="{Binding CurrentUser.UserName, StringFormat='User: {0}'}" FontWeight="SemiBold" Margin="16,16,16,0"/>
                <TextBlock DockPanel.Dock="Top" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" Text="{Binding CurrentUser.Role, StringFormat='Role: {0}'}" Margin="16,4,16,8" FontStyle="Italic"/>
                <Separator DockPanel.Dock="Top" Margin="8,8"/>

                <ListBox ItemsSource="{Binding NavigationItems}" 
                         SelectedItem="{Binding SelectedNavigationItem}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         BorderThickness="0"
                         Style="{StaticResource MaterialDesignNavigationListBox}">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:NavigationItemViewModel}">
                            <StackPanel Orientation="Horizontal" Margin="16,10">
                                <TextBlock Text="{Binding DisplayName}" Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentViewViewModel}" Margin="16"/>
        <materialDesign:Snackbar
            Grid.ColumnSpan="2"
            MessageQueue="{Binding SnackbarMessageQueue}"
            HorizontalAlignment="Center" 
            VerticalAlignment="Bottom" Margin="15"/>

    </Grid>
    
</Window>